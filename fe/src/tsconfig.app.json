{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": ["node"],
    "typeRoots": ["./node_modules/@types", "./local-typings"]

  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts",
  ]
}